/** @type {import('next').NextConfig} */
const nextConfig = {
  //   webpack(config) {
  //     config.resolve.fallback = {
  //       ...config.resolve.fallback,
  //       fs: false, // the solution
  //     };
  //     return config;
  //   },
};

module.exports = nextConfig;
